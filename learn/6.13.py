import torch as np
import torch
#print(x.shape)
#print(x.numel())
#print(x.reshape(3, 4))
#print(torch.ones((2, 3, 4)))
#print(np.tensor([[[1, 2, 3],[1, 2, 3]]]))
# print(np.tensor([[[1, 2], [1, 2],[1, 2]],[[1, 2], [1, 2],[1, 2]]]).shape)
#print(torch.tensor([1, 2, 3]))
'''x = np.tensor([1.0, 2, 4, 8])
y = torch.tensor([[[1, 2], [1, 2],[1, 2]],[[1, 2], [1, 2],[1, 2]]])
print(y)
y[0, 2] = 9
print(y)'''
a = torch.tensor([3.5])
'''# 按行求和（对每一列的元素求和）
row_sum = torch.sum(x, dim=0)  # 结果为 tensor([4, 6])

# 按列求和（对每一行的元素求和）
col_sum = torch.sum(x, dim=1)'''
